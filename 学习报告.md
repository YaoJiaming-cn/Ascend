# 昇腾AI架构学习与实践报告
## —— 基于 Ascend 910B 的计算机视觉模型迁移与训练

---

### 1. 昇腾全栈架构理解

昇腾AI处理器的全栈架构可分为四层，与NVIDIA GPU生态形成如下对应关系：

| 层次 | NVIDIA 生态 | 昇腾生态 | 作用 |
|------|------------|---------|------|
| 硬件 | NVIDIA GPU (Ampere/Hopper) | 达芬奇架构 (Ascend 910B) | 物理算力芯片 |
| 计算架构 | CUDA + cuDNN + TensorRT | CANN (异构计算架构) | 驱动、算子库、编译器 |
| 框架适配 | torch.cuda (PyTorch内置) | torch_npu (独立插件) | 使AI框架调用底层算力 |
| AI框架 | PyTorch / TensorFlow | PyTorch + torch_npu / MindSpore | 模型开发接口 |

**关键理解**：昇腾的软件栈设计思路与CUDA不同——PyTorch对CUDA的支持是原生的（`torch.cuda`内置），而对NPU的支持通过独立的`torch_npu`插件实现。在代码层面，迁移的核心变更仅为 `import torch_npu` 和使用 `.to('npu:0')` 替代 `.to('cuda:0')`。此外，NPU采用异步执行模式，性能计时需显式调用 `torch_npu.npu.synchronize()` 进行同步。

昇腾 910B 的FP16峰值算力为320 TFLOPS，与NVIDIA A100（312 TFLOPS）处于同一量级。本文所有实验均在华为云ModelArts平台搭载的单卡Ascend 910B4实例上完成。

---

### 2. 开发环境搭建与踩坑记录

**2.1 环境选型过程**

最初尝试了AtomGit AI提供的免费昇腾Notebook（云端Ascend 910B4，限时2小时），但在训练阶段频繁遇到共享内存不足（Bus error）和kernel自动重启的问题，稳定性无法满足完整训练需求。后续转向华为云ModelArts付费实例（单卡snt9b1，约18.68元/小时），使用预置镜像 `pytorch_2.7.1-cann_8.5.2`，开箱即用，大大降低了环境配置成本。这一过程也反映了当前昇腾开发者生态的现状：免费资源尚不稳定，生产级使用仍需依赖付费云服务。

**2.2 关键问题与解决方案**

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| DataLoader Bus error | 免费容器共享内存（/dev/shm）不足 | 切换到付费实例，增大num_workers |
| CIFAR-10下载极慢（~10KB/s） | pytorch.org服务器在海外 | 通过VPN加拿大节点下载后上传 |
| CANN SyntaxWarning刷屏 | CANN工具链部分Python代码使用了旧式字符串转义 | 设置sys.warnoptions屏蔽 |
| 预训练模型首轮即收敛（acc 98.7%） | 预训练ResNet-50对CIFAR-10过于强大 | 改用from scratch训练，调整学习率 |

---

### 3. ResNet-50 在昇腾NPU上的训练实验

**3.1 实验设置**

| 配置项 | 值 |
|--------|-----|
| 模型 | ResNet-50（随机初始化） |
| 数据集 | CIFAR-10（50k训练 / 10k测试） |
| 优化器 | SGD，lr=0.05，momentum=0.9 |
| 学习率策略 | CosineAnnealingLR（T_max=15） |
| 损失函数 | CrossEntropyLoss |
| Batch size | 128 |
| 训练轮数 | 15 epochs |
| 硬件 | 1 × Ascend 910B4（29.5 GB HBM） |

**3.2 训练结果**

| 阶段 | 训练集准确率 | 验证集准确率 |
|------|-------------|-------------|
| Epoch 1 | 16.8% | 23.8% |
| Epoch 5 | 61.3% | 62.4% |
| Epoch 10 | 82.5% | 79.2% |
| Epoch 15 | 95.6% | 84.3%（最佳） |

总训练时间：43.3分钟（2599秒），平均每epoch约173秒。

**3.3 性能对比分析**

| 设备 | 单batch训练耗时 | 加速比 |
|------|---------------|--------|
| CPU（鲲鹏920 ARM） | 53.5s | 1× |
| NPU（Ascend 910B4） | 0.42s | **127.6×** |

**分析**：NPU在训练加速方面表现优异（127倍于CPU）。但需要注意，当前实验使用FP32全精度训练，未启用混合精度（FP16）和更大的batch size。参考同类实验数据，若开启AMP自动混合精度并将batch size增大至256，预期epoch耗时可从173s降至约90-100s，进一步接近A100在同等配置下的性能水平（约60-80s/epoch）。

训练集与验证集准确率存在约11个百分点的差距（95.6% vs 84.3%），这表明模型存在一定程度的过拟合。后续可通过添加Dropout、更强的数据增强（如Cutout、Mixup）来缩小这一差距。

---

### 4. YOLOv8 目标检测在昇腾NPU上的迁移与训练



**4.1 实验背景与目的**

在完成 ResNet-50 图像分类任务后，进一步探索昇腾 NPU 在目标检测领域的表现。YOLOv8 是 Ultralytics 推出的主流单阶段检测模型，相比 ResNet-50 分类任务，目标检测涉及更复杂的损失函数（box loss + cls loss + dfl loss）、NMS 后处理和多尺度特征融合，对算力要求和框架兼容性都更高。本实验的核心目的：

- 验证 ultralytics 训练框架在昇腾 NPU 上的兼容性
- 完成完整的「训练 → 验证 → 推理」流程
- 记录 NPU 迁移中的特殊踩坑（与分类任务对比）

实验策略上，先在本地 RTX 3060 GPU 上用最小配置（batch=8, workers=0, epochs=10）验证代码逻辑无误，再上传至华为云 Ascend 910B4 实例完整复现。这样做的好处是：本地调试免费且快速，确保代码稳定后再在按小时计费的 NPU 上跑，避免因脚本 bug 浪费云资源。

**4.2 实验设置**

| 配置项 | 本地（验证场） | 云端（正式实验） |
|--------|--------------|----------------|
| 模型 | YOLOv8n（3.15M 参数，8.9 GFLOPs） | 同左 |
| 预训练权重 | yolov8n.pt（COCO 预训练） | 同左 |
| 数据集 | COCO128（128张图，80类） | 同左 |
| 训练轮数 | 10 epochs | 10 epochs |
| 图片尺寸 | 320×320 | 320×320 |
| Batch size | 8 | 8 |
| 优化器 | AdamW（auto） | AdamW（auto） |
| 混合精度 | AMP（自动） | 关闭（AMP 不兼容 NPU） |
| 设备 | CUDA:0（RTX 3060 6GB） | NPU:0（Ascend 910B4 29.5GB） |
| DataLoader workers | 0（Windows 多进程限制） | 4 |

**4.3 本地 GPU 调试踩坑**

本地调试阶段遇到了两个典型问题，虽然与 NPU 无关，但反映了 PyTorch 在 Windows 平台上的常见麻烦：

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| Jupyter 运行训练时 PermissionError (DuplicateHandle) | Windows 下 Jupyter kernel 使用 spawn 方式创建子进程，ultralytics DataLoader 与之冲突 | 改用独立 `.py` 脚本在 PyCharm 中运行 |
| OpenMP 重复加载导致 crash（libiomp5md.dll） | PyTorch 和 OpenCV/ultralytics 各自打包了 OpenMP 运行时 | 脚本最顶部设置 `os.environ['KMP_DUPLICATE_LIB_OK'] = 'TRUE'` |
| model.val() 报 freeze_support RuntimeError | val() 内部创建新 DataLoader 子进程，Windows 要求主程序入口有 `if __name__ == '__main__':` 保护 | 将全部代码包进 `main()` 函数 + `if __name__ == '__main__'` 守卫 |
| protobuf 警告刷屏 | Windows 上 protobuf C++ 实现不可用，导入 onnx 时触发 Fallback | 设置 `PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python` |

本地训练成功跑通后，mAP@50 = 0.595，mAP@50-95 = 0.429，与云端 NPU 结果基本一致，验证了代码的可靠性。

**4.4 NPU 训练过程与结果**

将代码迁移到云端 NPU 后，并非简单的 `cuda:0 → npu:0` 就能跑通。ultralytics 8.4.60 内部有两处硬编码了 `torch.cuda` 调用：

- `ultralytics/utils/checks.py` 的 `check_amp()` 函数：调用 `torch.cuda.get_device_name()` 检测设备，在未编译 CUDA 的 PyTorch 环境中直接 `AssertionError`
- `ultralytics/engine/trainer.py` 的 `_clear_memory()` 方法：每个 epoch 结束后调用 `torch.cuda.get_device_properties()` 检查显存，同源崩溃

第一处通过设置 `amp=False` 跳过；第二处通过在导入 YOLO 前 monkey-patch `BaseTrainer._clear_memory`，对 NPU 设备直接返回不检查。这种绕过方式虽然不够优雅，但在框架未完全适配的情况下是有效的临时方案。

训练过程 10 个 epoch 总耗时约 2.2 分钟，各 epoch 指标变化如下：

| Epoch | box_loss | cls_loss | dfl_loss | mAP@50 | mAP@50-95 |
|-------|----------|----------|----------|--------|-----------|
| 1 | 1.344 | 1.868 | 1.207 | 0.525 | 0.388 |
| 3 | 1.234 | 1.587 | 1.136 | 0.543 | 0.396 |
| 5 | 1.237 | 1.488 | 1.153 | 0.569 | 0.412 |
| 7 | 1.245 | 1.438 | 1.153 | 0.569 | 0.417 |
| 10 | 1.239 | 1.294 | 1.154 | **0.590** | **0.435** |

训练损失稳步下降，mAP 持续提升。最终 mAP@50=0.590，mAP@50-95=0.435。考虑到 coco128 仅 128 张图且只训练 10 轮，这个结果合理——预训练权重已经提供了强大的基础特征提取能力，微调主要是在适应数据分布。

训练过程中观察到的 NPU 推理指标：预处理 0.2ms，纯前向推理 0.6ms，损失计算 0.0ms，NMS 后处理 5.7ms。可见 NPU 在矩阵运算上表现优异（0.6ms 推理），但 NMS 算子因 `torchvision::nms` 不在 NPU 支持列表中而回退 CPU 执行，成为端到端流程的瓶颈。

**4.5 NPU 推理性能测试**

加载训练产出的 best.pt，对 test_bus.jpg 进行 50 次推理计时（含 5 次预热）：

| 指标 | 数值 |
|------|------|
| 单张端到端推理 | **26.2 ms** |
| FPS | **38.1** |
| 纯 NPU 前向推理 | 0.6 ms/张（训练时 val） |

端到端的 26.2ms 中，大部分消耗在 CPU 侧的 NMS 后处理上。纯 NPU 推理仅需 0.6ms，说明 Ascend 910B4 的算力对于 YOLOv8n 这种轻量模型完全足够。若后续使用 ATC 工具链将模型转为 .om 离线格式并结合 DVPP 预处理，有望进一步缩小端到端延迟。

**4.6 NPU 迁移关键踩坑总结**

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| ultralytics `check_amp()` 崩溃 | 调用 `torch.cuda.get_device_name()`，NPU 环境无 CUDA | 设置 `amp=False` 跳过混合精度检查 |
| `_clear_memory()` 崩溃 | 调用 `torch.cuda.get_device_properties()` 检查显存 | monkey-patch 该函数，NPU 设备直接 return |
| `model.val()` 回退 CPU | 训练结束后模型默认回到 CPU，val() 未指定 device | 显式传 `device='npu:0'` |
| torchvision::nms 不支持 NPU | CANN 算子库未覆盖 NMS 算子 | 自动 CPU fallback，后处理耗时增加 |
| Arial.ttf 字体下载失败 | 云端外网受限，ultralytics 尝试下载字体文件 | 不影响推理结果，仅标注文字用默认字体 |
| CANN SyntaxWarning 刷屏 | Python 3.12 对无效转义序列更严格（同 ResNet 实验） | 已习惯，不影响运行 |

与 ResNet-50 实验的对比：ResNet 分类任务（torchvision 原生模型 + 手写训练循环）在 NPU 上几乎零适配成本，仅需 `import torch_npu` + `.to('npu:0')`。而 YOLOv8 依赖第三方框架 ultralytics，框架内部的 CUDA 假设成为迁移的主要障碍。这反映了当前昇腾生态的一个现实：越接近「原生 PyTorch」的代码迁移越顺畅，而依赖复杂第三方框架时可能需要手动介入。

训练输出文件（训练曲线、混淆矩阵、PR 曲线、验证样本可视化等）见 `train_output.zip`。

---

### 5. 学习体会与总结

**5.1 昇腾生态的优势**
- 硬件算力与A100同级别，FP16纯算力甚至略有优势
- PyTorch迁移成本低——`import torch_npu` 后API几乎无变化
- 华为云ModelArts预置镜像开箱即用，省去繁复的环境配置
- 云端按需付费灵活，适合学生学习和原型验证

**5.2 当前存在的不足**
- 免费学习资源（如AtomGit Notebook）稳定性有待提升
- CANN工具链仍有一些小瑕疵（如Python版本兼容性带来的警告刷屏）
- 社区资料和踩坑分享相比CUDA生态仍有差距，部分问题需要自行摸索
- 算子覆盖尚未达到CUDA的完备程度，部分模型迁移仍需手动适配

**5.3 后续计划**
- ~~完成YOLOv8目标检测模型的NPU迁移与推理测试~~ ✓ 已完成
- 探索ATC离线模型转换（PyTorch → ONNX → OM），利用 NPU 原生算子替代 CPU fallback（如 NMS）
- 尝试DVPP数字视觉预处理模块，替代torchvision transforms以充分发挥NPU的编解码硬件加速能力

---

### 附录 A：实验环境详情

| 项目 | 信息 |
|------|------|
| NPU型号 | Ascend 910B4 |
| NPU显存 | 29.5 GB HBM |
| CANN版本 | 8.5.2 |
| npu-smi版本 | 23.0.6 |
| PyTorch | 2.7.1+cpu |
| torch_npu | 2.7.1.post2 |
| Python | 3.12.0 |
| CPU架构 | aarch64（鲲鹏920） |
| 操作系统 | openEuler (HCE 2.0) |
| 云平台 | 华为云 ModelArts（西南-贵阳） |

### 附录 B：训练曲线图
见 training_curves.png（训练/验证 loss 与 accuracy 随epoch变化曲线）。
